What i learned/ did:

Learned:
You can add from the standard library using the keyword 'use'.
To create variables we use the keyword 'let name = value'.
Variables are immutable by defualt, if you want mutable variables add 'mut' after 'let' eg: 'let mut <name> = <value>'.
// are used for comments and continue until the line break.
Shadowing is what allows us to reuse variables rather than having forced to create new unique variables.
.trim() will remove any whitespace at the begining and end.

Did:
Walked trough the Chapter 2 Guessing Game.


