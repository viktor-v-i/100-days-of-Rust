What i learend / did:
- Methods that take additional parameters beyond '&self'
- Associated functions = function in 'impl' that don't take '&self'
- '::' syntax for calling associated function vs dot notation for calling methods
- 'Self' return type keeps 'impl' blocks resilient to renames
