What i did / learned:

- Tuple structs
- Accessing tuple structs with dot notation
- Structs that exist as types even without data (unit-like structs)
- Destructuring tuple structs 
-  Passing a tuple struct to a function by reference (&Color in signature, &white at call site)
 
