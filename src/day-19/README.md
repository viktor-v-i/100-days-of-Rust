What i learned / did:
- 'impl' blocks that associate methods with a struct
- '&mut self' = mutable borrow. For methods to modify the fields of instance
- '&self' = read-only borrow. Method can read but not modify the fields of instance
- Call syntax with dot notation 
