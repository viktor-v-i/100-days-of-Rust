What i did/ learned:
learned:
scalars implement the copy trait, gets pushed to the stack
because strings get pushed to the heap, they do not implement the copy trait -
move happens instead.
.clone() copies the heap data of the string.

did:
challenge day-09
