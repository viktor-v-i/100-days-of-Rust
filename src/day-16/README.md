What i learned / did:
- Using field init shorthand for struct and update syntax to create new instances from exitsting ones.
- .. Moves string fields but copies u32 fields (copy trait, stack vs heap).
- Connecting ownership rules to struct fileds. 
