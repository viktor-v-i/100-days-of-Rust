What i learned / did:

- Matching on 'Option<T>' = pattern for "something or nothing"
- Two shapes of catch-all: 'other' binds the value, usable in the arm / '_' avoids the warning because the value is unused
- Compiler's exhaustiveness check
