What i learn / did:
- [derive(Debug)] enables debug printing for custom types:
	 '{:?}' = single-line,
	 '{:#?}' = multi-line,
	 'dbg!' = macro that prints to stderr with file:line
- Borrowing structs in functions to avoid losing ownership
- \n newline escape and bare println!() for blank lines
