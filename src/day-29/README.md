What i learned / did:
- A single package can contain multiple crates
- The binary crate uses the library crate by referring to it via the package name (e.g., day_29)
- Calling functions defined in another crate
