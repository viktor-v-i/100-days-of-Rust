What i learned/ did:

What i learned:
What type annotations are in rust and how to declare them.
Unsigned integers are positive while signed integers (having a sign before the integer) can be negative.
The different Scalar Data Types such as:
Integer Types: A number without a fractional component.
Floating-Point Types: A number including decimal points.
The Boolean Type: True and False.
The Character Type can hold any single & special character.
Compound Types which includes Tuples and Arrays.
Tuples can hold mixed types, example: let tup: (i32, u32, f32) <- that's the different types it can hold while-
Arrays holds the same type, example: let a: [i32; 5] <- the type is i32 containing 5 elements. //examples from book
I also learned how to manipulate the Compound Types to, for example, extract only one value from the list.
Scalar types are fixed in length and represent only one value.
Use 0x for hex literal syntax instead of #.

What i did:
The day-03 challenge.
