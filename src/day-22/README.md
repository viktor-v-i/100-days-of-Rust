What i learned / did:

- Declaring enum types
- Plain and data-carrying enums (like 'Red' and 'V4(String)')
- :: syntax for reaching into an enum's namespace 
- Functions that take an enum type as a parameter
