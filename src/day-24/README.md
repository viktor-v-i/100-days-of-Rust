What i learend / did:

- 'Option<T>' as the "value or nothing" type
- 'Some' / 'None' are values; 'Option<i32> / 'Option<char>' are types
- 'Option<T>' and 'T' are different types: 
	The compiler prevents you from using one where the other is expected without unwrapping first
